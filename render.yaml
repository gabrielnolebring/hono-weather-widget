services:
  - type: web
    name: hono-weather-widget
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python -m playwright install --with-deps
    startCommand: gunicorn app:app
